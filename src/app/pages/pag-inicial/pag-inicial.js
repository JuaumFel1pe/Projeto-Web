// Arquivo de scripts para interações futuras
document.addEventListener('DOMContentLoaded', () => {
    const botaoContato = document.getElementById('botao-contato');

    botaoContato.addEventListener('click', (event) => {
        // Exemplo de ação ao clicar:
        // event.preventDefault();
        // alert('Botão clicado!');
    });
});